let producten = ['keyboard', 'kaas', 'mousepad'];
let weekdagen = ['maandag', 'dinsdag', 'woensdag', 'donderdag', 'vrijdag', 'zaterdag', 'zondag'];

document.getElementById('hoofding');
document.getElementsByClassName('pxl') [0].innerHTML = 'PXL';

for (let i = 0; i < weekdagen.length; i++) {
    //do something
}

let producten = [
    {naam: 'appel', prijs: 5},
    {naam: 'appelsien', prijs: 2},
    {naam: 'peer', prijs: 1}
]
